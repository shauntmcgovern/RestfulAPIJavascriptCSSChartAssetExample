"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// index.ts
var index_exports = {};
__export(index_exports, {
  DictionaryProvider: () => DictionaryProvider,
  dictionaryContext: () => dictionaryContext,
  useDictionary: () => useDictionary,
  useTranslate: () => useTranslate,
  useTranslation: () => useTranslation
});
module.exports = __toCommonJS(index_exports);

// components/DictionaryProvider.tsx
var import_react2 = require("react");

// contexts/dictionaryContext.ts
var import_react = require("react");
var dictionaryContext = (0, import_react.createContext)({
  locale: null,
  entries: /* @__PURE__ */ new Map([
    [null, /* @__PURE__ */ new Map()]
  ])
});

// components/DictionaryProvider.tsx
var import_jsx_runtime = require("react/jsx-runtime");
var DictionaryProvider = ({
  locale = null,
  dictionaries,
  children,
  fallback
}) => {
  const map = (0, import_react2.useMemo)(
    () => new Map(
      Object.entries(dictionaries).map(([dictionaryLocale, translations]) => [
        dictionaryLocale,
        new Map(
          Object.entries(translations)
        )
      ])
    ),
    [dictionaries]
  );
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    dictionaryContext.Provider,
    {
      value: {
        locale,
        entries: map,
        fallback
      },
      children
    }
  );
};

// hooks/useDictionary.ts
var import_react3 = require("react");
var useDictionary = (locale) => {
  const { entries, locale: currentLocale } = (0, import_react3.useContext)(dictionaryContext);
  return entries.get(locale ?? currentLocale);
};

// hooks/useTranslate.ts
var import_react4 = require("react");
var useTranslate = (locale) => {
  const dictionary = useDictionary(locale);
  const { fallback } = (0, import_react4.useContext)(dictionaryContext);
  return (0, import_react4.useCallback)(
    (key) => {
      return dictionary.get(key) ?? fallback?.(key);
    },
    [dictionary, fallback]
  );
};

// hooks/useTranslation.ts
var import_react5 = require("react");
var useTranslation = (key, locale) => {
  const dictionary = useDictionary(locale);
  const { fallback } = (0, import_react5.useContext)(dictionaryContext);
  return dictionary.get(key) ?? fallback?.(key);
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  DictionaryProvider,
  dictionaryContext,
  useDictionary,
  useTranslate,
  useTranslation
});
