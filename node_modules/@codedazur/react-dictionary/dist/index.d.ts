import * as react from 'react';
import { ReactNode, FunctionComponent } from 'react';

type Dictionaries = ReadonlyMap<Locale | null, Dictionary>;
type Dictionary = ReadonlyMap<DictionaryKey, string>;
/**
 * This type can be overridden using module augmentation.
 *
 * @example
 * // dictionary.d.ts
 *
 * import "@codedazur/react-dictionary";
 *
 * declare module "@codedazur/react-dictionary" {
 *   export type DictionaryKey = "foo" | "bar" | "baz";
 * }
 */
type DictionaryKey = string;
/**
 * This type can be overridden using module augmentation.
 *
 * @example
 * // dictionary.d.ts
 *
 * import "@codedazur/react-dictionary";
 *
 * declare module "@codedazur/react-dictionary" {
 *   export type Locale = "en_US" | "en_GB";
 * }
 */
type Locale = string;
interface DictionaryContext {
    locale: Locale | null;
    entries: Dictionaries;
    fallback?: (key: string) => string;
}
declare const dictionaryContext: react.Context<DictionaryContext>;

interface DictionaryProviderProps {
    locale?: Locale;
    dictionaries: Record<Locale, Record<DictionaryKey, string>>;
    children: ReactNode;
    fallback?: (key: string) => string;
}
declare const DictionaryProvider: FunctionComponent<DictionaryProviderProps>;

declare const useDictionary: (locale?: Locale) => Dictionary;

declare const useTranslate: (locale?: Locale) => (key: DictionaryKey) => string | undefined;

declare const useTranslation: (key: DictionaryKey, locale?: Locale) => string | undefined;

export { type Dictionaries, type Dictionary, type DictionaryContext, type DictionaryKey, DictionaryProvider, type DictionaryProviderProps, type Locale, dictionaryContext, useDictionary, useTranslate, useTranslation };
